# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Client {
  """Direcciones del cliente"""
  addresses: [ClientAddress!]!

  """Email del cliente"""
  email: String!

  """Fecha de registro del cliente"""
  fecha_registro: DateTime!

  """ID único del cliente"""
  id: Int!

  """Indica si el cliente ha sido eliminado (soft delete)"""
  isDeleted: Boolean!

  """Nombre del cliente"""
  nombre: String!

  """Preferencias del cliente"""
  preferences: ClientPreferences

  """Teléfono del cliente"""
  telefono: String!
}

type ClientAddress {
  """Calle y número"""
  calle: String!

  """Ciudad"""
  ciudad: String!

  """Cliente propietario de esta dirección"""
  client: Client!

  """ID del cliente"""
  cliente_id: Int!

  """Código postal"""
  codigo_postal: String!

  """Indica si es la dirección principal del cliente"""
  es_principal: Boolean!

  """Estado o provincia"""
  estado_provincia: String!

  """Fecha de creación de la dirección"""
  fecha_creacion: DateTime!

  """ID único de la dirección"""
  id: Int!

  """Indica si la dirección ha sido eliminada (soft delete)"""
  is_deleted: Boolean!

  """Nombre o alias de la sucursal/dirección"""
  nombre_sucursal: String

  """País"""
  pais: String!
}

type ClientPreferences {
  cliente_id: Int!
  id: Int!
  idioma: String!
  notificaciones_email: Boolean!
  notificaciones_push: Boolean!
}

input CreateClientAddressInput {
  """Calle y número"""
  calle: String!

  """Ciudad"""
  ciudad: String!

  """ID del cliente"""
  cliente_id: Int!

  """Código postal"""
  codigo_postal: String!

  """Indica si es la dirección principal"""
  es_principal: Boolean! = false

  """Estado o provincia"""
  estado_provincia: String!

  """Nombre o alias de la sucursal/dirección"""
  nombre_sucursal: String

  """País"""
  pais: String! = "Costa Rica"
}

input CreateClientInput {
  """Email del cliente"""
  email: String!

  """Nombre del cliente"""
  nombre: String!

  """Teléfono del cliente"""
  telefono: String!
}

input CreateClientPreferencesInput {
  cliente_id: Int!
  idioma: String
  notificaciones_email: Boolean
  notificaciones_push: Boolean
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Mutation {
  createClient(createClientInput: CreateClientInput!): Client!

  """Crear una nueva dirección para un cliente"""
  createClientAddress(createClientAddressInput: CreateClientAddressInput!): ClientAddress!
  createClientPreferences(createClientPreferencesInput: CreateClientPreferencesInput!): ClientPreferences!
  permanentDeleteClient(id: Int!): Boolean!
  removeClient(id: Int!): Client!

  """Eliminar una dirección (soft delete)"""
  removeClientAddress(id: Int!): ClientAddress!
  removeClientPreferences(id: Int!): ClientPreferences!
  restoreClient(id: Int!): Client!

  """Establecer una dirección como principal para el cliente"""
  setPrincipalAddress(id: Int!): ClientAddress!
  updateClient(updateClientInput: UpdateClientInput!): Client!

  """Actualizar una dirección existente"""
  updateClientAddress(updateClientAddressInput: UpdateClientAddressInput!): ClientAddress!
  updateClientPreferences(updateClientPreferencesInput: UpdateClientPreferencesInput!): ClientPreferences!
}

type PaginatedClientAddresses {
  clientId: Int!
  data: [ClientAddress!]!
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  limit: Int!
  page: Int!
  total: Int!
  totalPages: Int!
}

type PaginatedClients {
  data: [Client!]!
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  limit: Int!
  page: Int!
  total: Int!
  totalPages: Int!
}

input PaginationInput {
  limit: Int = 10
  page: Int = 1
}

type Query {
  allClientsIncludingDeleted: [Client!]!
  client(id: Int!): Client!

  """Obtener una dirección específica por su ID"""
  clientAddress(id: Int!): ClientAddress!

  """Obtener todas las direcciones de clientes"""
  clientAddresses: [ClientAddress!]!

  """Obtener todas las direcciones de un cliente específico"""
  clientAddressesByClient(clientId: Int!): [ClientAddress!]!

  """Obtener las direcciones de un cliente específico con paginación"""
  clientAddressesByClientPaginated(clientId: Int!, paginationInput: PaginationInput): PaginatedClientAddresses!
  clientPreference(id: Int!): ClientPreferences!
  clientPreferences: [ClientPreferences!]!
  clientPreferencesByClientId(clientId: Int!): ClientPreferences
  clients: [Client!]!
  clientsPaginated(paginationInput: PaginationInput): PaginatedClients!
}

input UpdateClientAddressInput {
  """Calle y número"""
  calle: String

  """Ciudad"""
  ciudad: String

  """ID del cliente"""
  cliente_id: Int

  """Código postal"""
  codigo_postal: String

  """Indica si es la dirección principal"""
  es_principal: Boolean

  """Estado o provincia"""
  estado_provincia: String

  """ID de la dirección a actualizar"""
  id: Int!

  """Nombre o alias de la sucursal/dirección"""
  nombre_sucursal: String

  """País"""
  pais: String
}

input UpdateClientInput {
  """Email del cliente"""
  email: String
  id: Int!

  """Nombre del cliente"""
  nombre: String

  """Teléfono del cliente"""
  telefono: String
}

input UpdateClientPreferencesInput {
  cliente_id: Int
  id: Int!
  idioma: String
  notificaciones_email: Boolean
  notificaciones_push: Boolean
}